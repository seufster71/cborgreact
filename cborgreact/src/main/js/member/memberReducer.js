export default function memberReducer(state = {}, action) {
  switch(action.type) {
    case 'LOAD_INIT_MEMBER': {
      let myState = {};
      break;
    }
      default:
        return state;
    }
}
